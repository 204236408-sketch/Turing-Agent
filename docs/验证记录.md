# 验证记录

日期：2026-06-23

| 检查项 | 状态 | 说明 |
|---|---|---|
| PDF 技术栈和目录映射 | verified | 已按原生前端、FastAPI、MySQL、ChromaDB、LangChain/LangGraph、PaddleOCR 重构 |
| Python 语法编译 | verified | `backend/` 与 `tests/` 通过 `compileall` |
| Seed JSON 格式 | verified | 6 个初始化 JSON 均可解析 |
| 核心目录与文件 | verified | 前端、后端、数据库、部署、测试入口均存在 |
| FastAPI 运行测试 | partial | 当前工作环境尚未安装 `backend/requirements.txt` |
| Pytest 执行 | partial | 当前工作环境尚未安装 pytest，测试骨架已经建立 |

安装后执行：

```bash
cd backend
python -m pip install -r requirements.txt
uvicorn main:app --reload
```

另开终端执行：

```bash
python -m pytest -q
```

