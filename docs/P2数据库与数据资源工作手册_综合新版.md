# P2 数据库与数据资源工作手册（综合新版）

> 角色定位：数据库设计、MySQL schema、种子数据、知识库、题库、视频资源、数据迁移、备份。  
> 上游：PDF 技术方案、差距审计、全量规则文档。  
> 下游：P3 Agent/RAG/爬虫、P1 API、P4 页面、P5 测试。
>
> 注意：本版尽量保留原 P2 Day 1 工作，只增强表结构完整性和数据格式要求。

---

## 1. P2 整体负责范围

P2 是五人流水线最上游。P2 不只“写点 JSON”，而是要保证后面所有智能逻辑都有真实数据可以用。

| 负责部分 | 具体内容 | 文件 |
|---|---|---|
| MySQL 表结构 | 对齐 `backend/models.py` 和新增需求，补全 MySQL schema | `database/schema.sql`、`database/migrations/*.sql`、`backend/models.py` |
| 知识点 | 408 四科知识点、父子关系、关键词、常见错因 | `backend/data/seed_knowledge_points.json` |
| 题库 | 选择题、填空、简答、综合题、答案、解析、提示 | `backend/data/seed_questions.json` |
| 视频资源 | 高频知识点公开视频资源 | `backend/data/seed_video_resources.json` |
| 知识库文档 | RAG/ChromaDB 用 Markdown 文档 | `backend/data/knowledge_docs/*.md` |
| 论坛数据 | 分类、演示帖子、知识点关联 | `backend/data/seed_forum_categories.json`、`backend/data/seed_forum_posts.json` |
| 导入脚本 | 全量种子数据导入 | `backend/scripts/seed_data.py` |
| 迁移脚本 | SQLite 到 MySQL | `backend/scripts/migrate_sqlite_to_mysql.py` |
| 备份 | MySQL 备份 | `deploy/backup_mysql.sh` |

---

## 2. 上下游先后顺序

```text
P2 先给数据和表
→ P3 根据数据写 Agent、RAG、爬虫、推荐逻辑
→ P1 根据表和字段写接口
→ P4 根据接口展示数据
→ P5 根据数据写测试
```

每天必须交给下游：

- 新增或修改了哪些表。
- 新增或修改了哪些字段。
- JSON 格式是否变化。
- 数据量是否达到当天验收标准。
- 字段名称是否与前一天不同。

---

## 3. 数据命名和格式总规则

### 3.1 科目名称固定

只能使用：

```text
数据结构
计算机组成原理
操作系统
计算机网络
```

不能混写：

```text
计组
组成原理
OS
网络
```

### 3.2 知识点名称全局唯一

例如统一使用：

```text
页面置换算法
```

不要混用：

```text
页置换
LRU算法
页面替换
```

### 3.3 JSON 字段规则

所有 JSON 使用 UTF-8，字段名统一英文 snake_case。

布尔值：

```json
"is_high_frequency": true
```

数组字段：

```json
"keywords": ["LRU", "FIFO", "缺页次数"]
```

如果当前导入脚本暂不支持数组，P2 需要同步修改 `seed_data.py`，不能要求 P3/P1 猜格式。

---

## 4. 每天工作安排

## Day 1：补齐 MySQL schema 和知识点数据

> 这一天尽量沿用原 P2 工作：对照模型补 schema，扩充知识点。新增要求是必须把 PDF 差距审计中缺的生产表补进去。

### 上午要做

修改：

```text
backend/models.py
database/schema.sql
database/migrations/*.sql
```

需要补齐模型和表：

```text
FavoriteQuestion
QuestionInteraction
UserDailyActivity
UserPendingRecommendation
ForumLike
ForumCollect
ForumAiUsage
VideoCrawlLog
VideoViewLog
KnowledgeDocument
ReportExportLog
```

`favorite_question` 建议：

```sql
CREATE TABLE favorite_question (
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_id INT NOT NULL,
  question_id INT NOT NULL,
  status VARCHAR(32) DEFAULT 'active',
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uniq_user_question(user_id, question_id)
);
```

`user_daily_activity` 建议：

```sql
CREATE TABLE user_daily_activity (
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_id INT NOT NULL,
  activity_date VARCHAR(10) NOT NULL,
  activity_type VARCHAR(64) NOT NULL,
  activity_count INT DEFAULT 1,
  source_id INT NULL,
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_user_date(user_id, activity_date)
);
```

`knowledge_document` 建议：

```sql
CREATE TABLE knowledge_document (
  id INT PRIMARY KEY AUTO_INCREMENT,
  subject VARCHAR(64) NOT NULL,
  knowledge_point VARCHAR(128) NOT NULL,
  title VARCHAR(180) DEFAULT '',
  source_file VARCHAR(255) DEFAULT '',
  section VARCHAR(64) DEFAULT '',
  content TEXT,
  keywords TEXT,
  chunk_id VARCHAR(128) DEFAULT '',
  vector_stored TINYINT DEFAULT 0,
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 下午要做

修改：

```text
backend/data/seed_knowledge_points.json
backend/scripts/seed_data.py
```

知识点格式：

```json
{
  "subject": "操作系统",
  "name": "页面置换算法",
  "parent_name": "内存管理",
  "level": 3,
  "content": "页面置换算法用于在缺页且页框已满时选择被换出的页面。",
  "common_mistakes": ["初始装入漏算缺页", "LRU 命中后忘记更新最近使用顺序"],
  "keywords": ["LRU", "FIFO", "OPT", "Clock", "缺页次数"],
  "is_high_frequency": true,
  "prerequisites": ["分页存储管理", "虚拟内存"]
}
```

最低数量：

- 总知识点不少于 40。
- 每科不少于 8。
- 高频不少于 20。

### 交付给谁

- 给 P3：知识点数据和常见错因，用于 RAG、出题、错因推荐。
- 给 P1：新增表和字段。
- 给 P5：可测试的数据量和样例。

### 完成标准

- MySQL 空库能执行 `schema.sql`。
- `seed_data.py` 能导入知识点。
- `knowledge_point` 表不少于 40 条。

---

## Day 2：知识库 Markdown、推荐规则、掌握度规则

### 上午要做：知识库文档

新增：

```text
backend/data/knowledge_docs/
backend/scripts/build_knowledge_docs.py
```

知识库文档命名：

```text
操作系统_页面置换算法.md
计算机网络_TCP连接释放.md
```

模板：

```md
# 页面置换算法

subject: 操作系统
knowledge_point: 页面置换算法
keywords: LRU,FIFO,OPT,Clock,缺页次数

## 核心概念
页面置换算法用于在发生缺页且物理页框已满时选择淘汰页面。

## 408 常见考法
- 给定访问序列计算缺页次数。
- 比较 FIFO、LRU、OPT 的替换过程。

## 解题步骤
1. 写出页框数量。
2. 按访问序列逐步模拟。
3. 命中时更新算法状态。
4. 未命中时计入缺页。

## 常见错误
- 初始装入没有计入缺页。
- LRU 命中后没有更新最近访问顺序。

## 例题
...
```

最低数量：

- 至少 20 份。
- 高频知识点优先。

### 下午要做：推荐和掌握规则

修改：

```text
backend/services/recommendation_service.py
backend/services/mastery_service.py
```

推荐评分：

```text
recommend_score =
  status_weight * 0.40
  + recency_weight * 0.20
  + frequency_weight * 0.15
  + weak_score_weight * 0.15
  + interval_weight * 0.10
```

掌握度 `weak_score`：

```text
wrong_count * 3
+ unfamiliar_count * 2
+ unknown_count * 4
+ ocr_mistake_count * 3
+ concept_error_count * 2
+ rule_confusion_count * 2
+ qa_count * 1
+ forum_count * 1
- correct_count * 1
- mastered_count * 2
```

知识点级状态优先级：

```text
无行为 -> 未学
weak_score >= 10 或 repeated_error_type >= 2 -> 薄弱点
unknown_count >= 1 或 correct_rate < 50% 或 continuous_wrong_count >= 2 -> 不会
unfamiliar_count >= 1 或 50% <= correct_rate < 80% 或 wrong_count in [1,2] -> 不熟
total_answer_count >= 3 且 correct_rate >= 80% 且 weak_score <= 2 -> 掌握
其他 -> 不熟
```

### 交付标准

- P3 可以导入知识库到 ChromaDB。
- 首页推荐排序有明确公式。
- 题本状态和知识点状态不再混用。

---

## Day 3：题库、活动表、题本测试数据

### 上午要做：题库

修改：

```text
backend/data/seed_questions.json
backend/scripts/seed_data.py
```

题目格式：

```json
{
  "subject": "操作系统",
  "knowledge_point": "页面置换算法",
  "difficulty": "中等",
  "question_type": "选择题",
  "variant_type": "choice",
  "question_text": "某系统为进程分配 3 个页框，访问序列为 1,2,3,1,4,2,5，采用 LRU 算法，缺页次数为多少？",
  "options": ["A. 4 次", "B. 5 次", "C. 6 次", "D. 7 次"],
  "standard_answer": "C",
  "explanation": "按 LRU 规则逐步模拟，初始装入也计入缺页。",
  "hints": ["先画 3 个页框", "命中后也要更新最近访问顺序", "统计每次未命中的访问"],
  "source": "seed"
}
```

最低要求：

- 至少 60 道题。
- 每科不少于 12 道。
- 每个高频点至少 3 道。
- 包含选择、填空、简答、综合题样例。

### 下午要做：题本和活动数据

确保以下表能导入和查询：

```text
favorite_question
user_pending_recommendation
user_daily_activity
mistake.mastery_status
answer_record.mastery_feedback
```

给 P5 准备测试样例：

```text
一个不熟题
一个不会题
一个已掌握题
一个可收藏题
一个可生成相似错题的知识点
```

### 完成标准

- 出题接口不会因为题库空而只靠 LLM。
- P4 错题本有真实数据可展示。
- P5 可测题本筛选。

---

## Day 4：视频、论坛、报告数据

### 上午要做：视频资源

修改：

```text
backend/data/seed_video_resources.json
```

视频格式：

```json
{
  "subject": "操作系统",
  "knowledge_point": "页面置换算法",
  "title": "408 操作系统：页面置换算法 FIFO/LRU/OPT",
  "platform": "Bilibili",
  "url": "https://www.bilibili.com/video/xxx",
  "reason": "覆盖缺页次数计算和 LRU 命中后更新顺序",
  "quality_score": 86,
  "duration": "18:20",
  "author": "408考研UP主",
  "crawl_source": "seed"
}
```

最低要求：

- 至少 80 条。
- 高频知识点每点至少 2 条。
- URL 必须人工检查能打开。

### 下午要做：论坛和画像

修改或新增：

```text
backend/data/seed_forum_categories.json
backend/data/seed_forum_posts.json
backend/services/tag_service.py
database/demo_data.sql
```

论坛帖子要求：

- 每科至少 5 条。
- 每条有 `subject` 和 `knowledge_point`。
- 至少包含 5 条适合 AI 小助手回答的提问帖。

`tag_service.py` 标签：

```text
连续学习 N 天
分步型学习者
计算易遗漏
概念型学习者
审题要仔细
规则混淆型
晨型学习者
夜间学习者
```

### 完成标准

- 视频接口大多数高频点不空。
- 论坛排行榜和报告画像有数据可测。

---

## Day 5：迁移、导入、备份、数据验收

### 上午要做

新增：

```text
backend/scripts/migrate_sqlite_to_mysql.py
```

迁移顺序：

```text
user, user_profile, subject, knowledge_point
question_generation_session, question, video_resource
answer_record, knowledge_mastery, mistake, user_memory
conversation, conversation_message
forum_category, forum_post, forum_comment
report
favorite_question, forum_like, forum_collect, user_daily_activity
```

每张表迁移后输出：

```text
table_name: sqlite_count -> mysql_count
```

### 下午要做

新增：

```text
deploy/backup_mysql.sh
docs/数据验收表.md
```

数据验收表：

```text
用户数量：
科目数量：
知识点数量：
题目数量：
视频数量：
知识库文档数量：
错题数量：
论坛帖子数量：
报告数量：
Chroma collection：
```

### 完成标准

- MySQL 空库可导入全量数据。
- SQLite 数据可迁移到 MySQL。
- 备份脚本可执行。

---

## 5. P2 每日自查清单

- JSON 是否合法？
- 科目名称是否统一？
- 知识点名称是否统一？
- 新字段是否同步到 `schema.sql`？
- 导入脚本是否兼容新格式？
- 是否把字段变化告诉 P1/P3/P5？

## 6. P2 最终交付物

- 完整 MySQL schema。
- 40+ 知识点。
- 60+ 题。
- 80+ 视频。
- 20+ 知识库 Markdown。
- 全量导入脚本。
- SQLite 到 MySQL 迁移脚本。
- MySQL 备份脚本。
- 数据验收表。
