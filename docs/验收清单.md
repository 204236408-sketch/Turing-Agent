# Day1 测试验收精简清单
## 一、基础环境
后端地址：http://127.0.0.1:8000
前端页面：http://127.0.0.1:8000/version-b.html
数据库：SQLite；LLM已启用；Chroma向量库关闭

## 二、自动化测试执行结果
1. 全量测试（总计11条用例）
通过10条，失败1条
失败用例：首页推荐接口
问题：预期返回「薄弱点强化」，实际返回「四科随机综合」，P1阻塞缺陷
警告：81条，仅废弃语法优化提示，不影响业务功能

2. 分模块专项测试
1）tests/test_smoke.py：全部用例执行通过
2）tests/test_health.py：屏蔽字段校验阻塞用例后，剩余用例全部通过
3）tests/test_auth.py：共13条用例，12条通过，1条失败
失败用例：无Token访问 /api/auth/me
失败原因：接口鉴权拦截功能异常，未携带JWT Token仍可正常获取用户信息，存在越权访问安全漏洞，P1级缺陷，需优先修复get_current_user鉴权依赖

## 三、八大模块GET接口故障汇总
| 模块 | GET访问状态 | 故障描述 | 缺陷等级 |
| ---- | ---- | ---- | ---- |
| 健康检查 | 200正常 | 返回数据缺失规范字段 | P1 |
| 登录认证 | 404 | 无GET兜底路由 | P1 |
| 首页模块 | 200正常 | 无Token鉴权拦截，匿名可读取私有数据（高危漏洞） | P1 |
| 出题模块 | 404 | 未配置GET基础路由 | P1 |
| 答题模块 | 404 | 未配置GET基础路由 | P1 |
| 错题模块 | 404 | 未配置GET基础路由 | P1 |
| 视频模块 | 404 | 未配置GET基础路由 | P1 |
| 论坛模块 | 404 | 未配置GET基础路由 | P1 |
| 报告模块 | 200正常 | 暂未深度校验返回字段 | P1 |

核心阻塞问题：6个业务模块缺失GET路由，无法开展后续自动化脚本开发

## 四、缺陷分级汇总
###  P1 高优先级阻塞缺陷（需优先修复）
1. 登录、出题、答题、错题、视频、论坛6个模块无GET路由，页面访问404
2. /api/auth/me、首页接口缺少Token鉴权，未登录可查看私有业务数据
3. 首页个性化推荐逻辑与需求不符
4. 健康接口返回字段缺失、登录失败无统一错误提示文案
5. 两处代码废弃语法，需迭代优化

###  P2 低优先级缺陷
数据库文件丢失时服务自动新建空白库，无数据库故障降级、异常返回逻辑

## 五、前端手工主流程测试
测试账号：demo@turing408.ai / 123456
流程：打开前端页面 → 账号密码登录 → 跳转首页
测试结果：页面正常渲染，无弹窗、无404/500接口报错

## 六、数据库降级异常场景测试
场景：修改配置指向不存在的数据库文件，重启服务访问健康接口
预期：接口返回ok:false，提示数据库故障
实际：程序自动新建空白数据库，接口正常返回成功，无故障兜底逻辑，校验不通过

## 七、交付物与自查项
1. 交付文件：tests全套自动化测试脚本、本验收文档
2. 自查全部完成：自动化用例编写、手工主流程验收、异常降级场景测试、缺陷分级归档

## 八、整体风险说明
1. 高风险：6个业务模块无GET路由，阻塞Day2、Day3自动化测试开发进度
2. 中风险：首页、个人信息接口无鉴权拦截，存在用户数据泄露安全漏洞
3. 低风险：废弃语法、接口字段缺失、数据库降级逻辑缺失，不影响基础业务正常使用


# Day2 接口自动化测试验收清单
## 一、首页个性化模块
测试文件：`tests/test_home_personalization.py`
对应后端接口文件：`routers/home_router.py`
接口地址：`/api/home/overview`
验收场景规范：新用户基线诊断、错题用户优先薄弱点、推荐列表携带subject/knowledge_point/count、统计卡片含trend、记忆为空返回空状态不报错

### 未通过测试用例明细（全部用例失败）
1. **test_home_overview_no_authorization_token**
    - 测试场景：未携带Token访问首页，校验无Token鉴权拦截、返回结构完整
    - 测试结果：未通过
    - 失败原因：后端倒计时逻辑混用无时区/带时区datetime，时间计算抛出`TypeError: can't subtract offset-naive and offset-aware datetimes`，接口直接崩溃
    - 后端修改方案：`routers/home_router.py` 删除倒计时ZoneInfo时区代码，统一使用无时区datetime计算时间
2. **test_home_overview_empty_new_user**
    - 测试场景：空白新用户（无错题/知识点/记忆），校验新用户基线诊断、空状态标识、全字段完整性
    - 测试结果：未通过
    - 失败原因：同上时区datetime冲突报错
    - 后端修改方案：同上
3. **test_home_overview_user_with_study_record**
    - 测试场景：存量学习用户（错题+薄弱知识点+长期记忆），校验薄弱点优先推荐、trend统计卡片、图谱数据输出
    - 测试结果：未通过
    - 失败原因：同上时区datetime冲突报错
    - 后端修改方案：同上
4. **test_home_countdown_format**
    - 测试场景：空白用户访问，校验考研倒计时字段结构、格式规范
    - 测试结果：未通过
    - 失败原因：同上时区datetime冲突报错
    - 后端修改方案：同上
5. **test_home_cross_origin_request**
    - 测试场景：前端携带Origin跨域头访问，校验跨域放行、无Token错误返回格式规范
    - 测试结果：未通过
    - 失败原因：同上时区datetime冲突报错
    - 后端修改方案：同上

### 首页模块总结缺陷
1. 缺陷等级：**P1阻塞**
2. 问题点：首页倒计时时间计算时区混用，首页接口完全不可用
3. 修复文件：`routers/home_router.py`
4. 修复要求：移除ZoneInfo时区相关代码，统一使用offset-naive datetime

---
## 二、知识图谱模块
测试文件：`tests/test_knowledge.py`、`tests/test_recommendation.py`、`tests/test_qa_graph.py`
对应后端接口文件：`routers/knowledge_router.py`、`routers/recommend_router.py`

### 2.1 接口 /api/knowledge/graph
验收场景规范：图谱语义检索、向量库离线自动降级MySQL关键词检索、无外部依赖不返回500、字段结构统一
- 测试结果：流程用例可执行通过，存在P1阻塞缺陷
#### 阻塞缺陷（P1）
1. 空知识点库返回`{"subjects": {}}`字典，规范要求空数组`[]`，前端列表渲染报错，图谱页面打不开
2. ChromaDB向量库离线无降级兜底逻辑，无`fallback_used`标识，外部依赖断开直接500崩溃，不满足验收标准
#### 正常项
匿名访问鉴权拦截逻辑正常，无越权漏洞

### 2.2 接口 /api/knowledge/high-frequency
验收场景规范：支持匿名访问、无数据返回标准空数组`items: []`，字段完整
- 测试结果：全部通过，无缺陷

### 2.3 接口 /api/recommend（test_recommendation.py）
验收场景规范：错题用户优先薄弱点、推荐列表携带subject/knowledge_point/count
#### 未通过测试用例明细
1. **test_recommend_empty_user**
    - 测试场景：空白新用户请求推荐接口，校验鉴权拦截、空推荐列表规范结构
    - 测试结果：未通过
    - 失败原因：鉴权失效，无Token匿名可直接获取推荐数据，未拦截401，存在数据越权安全风险
    - 后端修改方案：`routers/recommend_router.py` 接入全局Token鉴权拦截，无Token返回401
#### 优化缺陷（P3）
1. LLM服务断开无降级兜底逻辑，无`fallback_used`标识字段，容错机制缺失

### 知识图谱模块总结缺陷
1. P1阻塞：图谱空数据结构错误、向量库离线无降级；推荐接口全局鉴权失效，存在安全漏洞
2. P3优化：推荐接口LLM离线无降级标识
3. 修复文件：`routers/knowledge_router.py`、`routers/recommend_router.py`

---
## 三、QA/RAG问答模块
### 3.1 主问答接口 /api/qa/chat
测试文件：`tests/test_rag_fallback.py`
对应后端接口文件：`routers/qa_router.py`、`services/hybrid_retriever.py`、`agents/qa_agent.py`
验收场景规范：普通问题返回subject/knowledge_point/answer/agent_steps、追问继承上一轮知识点、LLM离线返回fallback、ChromaDB离线降级MySQL检索、agent_steps长度≥4、无外部依赖不返回500

#### 通过用例（仅标注场景，无失败信息）
1. test_rag_fallback_conversation_not_exist：传入不存在会话ID，自动新建会话、流程不中断、agent_steps≥4
2. test_rag_fallback_mastery_update_error：掌握度更新异常不阻断问答主流程，接口200正常返回
3. test_rag_fallback_no_retrieve_result：无关问题检索为空，用户记忆正常加载
4. test_rag_fallback_no_token_demo_user：无Token访问，鉴权拦截、返回结构合法
5. test_rag_fallback_multi_turn_followup_inherit_kp：多轮追问兼容空检索场景，流程正常执行

#### 未通过测试用例明细
1. **test_rag_fallback_llm_disabled**
    - 测试场景：清空LLM密钥模拟全局LLM禁用，校验降级兜底、`llm_used=False`、检索/记忆置空、兜底文案正确
    - 测试结果：未通过
    - 失败原因：全局异常兜底分支未赋值`llm_used=False`，异常返回该字段仍为True，断言不匹配
    - 后端修改方案：`routers/qa_router.py` 异常兜底返回体强制设置`llm_used=False`
2. **test_rag_fallback_llm_http_error**
    - 测试场景：mock LLM抛出504超时异常，校验异常捕获、降级兜底、`llm_used`、异常日志文案
    - 测试结果：未通过
    - 失败原因：同上`llm_used`字段赋值缺失
    - 后端修改方案：同上
3. **test_rag_fallback_chroma_empty**
    - 测试场景：清空向量库模拟ChromaDB离线，校验自动降级MySQL关键词检索、返回数据source仅mysql
    - 测试结果：未通过
    - 失败原因：`hybrid_retriever.py` 混合检索逻辑缺陷，向量库失效未执行MySQL检索，检索结果为空列表
    - 后端修改方案：`services/hybrid_retriever.py` 向量库访问失败分支强制执行MySQL关键词检索
4. **test_rag_fallback_memory_read_fail**
    - 测试场景：mock用户记忆读取抛出数据库异常，校验记忆返回空列表、知识库检索正常
    - 测试结果：未通过
    - 失败原因：`agents/qa_agent.py` 调用记忆读取无try-except捕获，异常未拦截，仍返回真实记忆数据
    - 后端修改方案：`agents/qa_agent.py` 给`retrieve_user_memory`增加异常捕获，异常返回空记忆数组

#### 主问答接口全局缺陷
##### P1阻塞缺陷
1. 鉴权漏洞：无Token匿名可直接调用问答接口，未返回401拦截，违反全局强制鉴权规范
2. 空提问无统一标准化兜底文案，前端弹窗展示混乱
##### P3优化缺陷
1. RAG返回体缺失`retrieved_context`检索字段，无法完整校验向量检索链路
2. LLM离线无`fallback_used`降级标识字段，兜底状态无法区分

### 3.2 会话上下文接口 /api/conversation/{id}/context
测试文件：`tests/test_conversation_context.py`
对应后端接口文件：`routers/conversation_router.py`
验收场景规范：context返回recent_messages(messages)、summary、followups三字段，多轮对话完整返回历史消息
- 测试结果：基础流程用例全部通过，字段校验类断言失败
#### 核心缺陷
1. P1阻塞：全会话接口鉴权失效，无Token可查看/修改他人会话，严重越权安全漏洞
2. P1阻塞：创建会话接口请求方式与文档不符，文档POST、后端仅支持GET，多轮问答会话创建流程中断
3. P3优化：
    - context接口顶层返回无`summary`、`followups`字段，不满足验收文档规范
    - 新建会话数据无法持久入库，会话列表查询不到，多轮上下文链路失效
    - 部分接口未统一`{"ok": bool, "data": {}, "msg": ""}`标准返回结构
    - 会话摘要接口LLM/数据库故障无降级兜底，无`fallback_used`标识

### QA/RAG模块总结缺陷
1. P1阻塞：
    - `qa_router.py` 异常兜底`llm_used`赋值错误；问答全局鉴权失效；空提问无统一文案；会话全模块鉴权失效、创建请求方法不匹配
    - `hybrid_retriever.py` 向量库离线无MySQL降级检索
2. P2优化：`qa_agent.py` 记忆读取无异常捕获
3. P3优化：缺失`retrieved_context`、`fallback_used`字段；会话context缺失summary/followups；会话无统一返回结构、无降级兜底

## 四、全量测试执行汇总
1. **首页模块**：5个用例全部未通过，根因时区datetime计算冲突（P1阻塞，整体不可用）
2. **知识图谱模块**：
    - test_knowledge.py：全部通过
    - test_qa_graph.py：全部通过
    - test_recommendation.py：1 failed / 5 passed，根因鉴权失效（P1阻塞）
3. **RAG问答模块 test_rag_fallback.py**：4 failed / 5 passed，3处后端业务逻辑缺陷（P1/P2）
4. **会话模块 test_conversation_context.py**：流程用例通过，字段校验断言失败（后端缺失规范字段）
### 通用警告说明
全项目大量`datetime.utcnow()` DeprecationWarning，属于SQLAlchemy底层库过期API警告，**不影响业务功能**，无需后端修复，仅IDE/控制台输出日志。


# DAY3测试验收总清单
## 一、出题工作流模块（question_graph）
测试文件：`tests/test_question_graph.py`
对应后端文件：`services/question_service.py`
核心接口：`POST /api/question/generate`

### 未通过用例明细
1. **test_question_graph_workflow_normal**
    - 场景：正常完整出题流程，校验参数、生成逻辑、返回结构
    - 结果：未通过
    - 失败原因：`generate_questions` 函数签名变更，仅支持1个位置参数，测试传入8个位置参数，触发参数数量TypeError
    - 后端修复：`question_service.py` 恢复多位置入参签名，兼容旧调用
2. **test_question_graph_llm_timeout_fallback**
    - 场景：LLM超时降级，校验接口不崩溃、返回空数组
    - 结果：未通过
    - 失败原因：`generate_questions` 移除db入参，测试传入db关键字参数无法识别，报TypeError
    - 后端修复：函数增加db兼容关键字参数，向下兼容旧调用
3. **test_question_graph_validate_mismatch_fallback**
    - 场景：知识点不匹配降级返回空列表
    - 结果：未通过
    - 失败原因：函数不识别db关键字参数，参数匹配报错
    - 后端修复：同上
4. **test_question_graph_fill_essay_comprehensive**
    - 场景：综合大题多题型生成，校验返回字段完整
    - 结果：未通过
    - 失败原因：函数不识别db关键字参数，参数匹配报错
    - 后端修复：同上
5. **test_question_graph_agent_steps_complete**
    - 场景：生成题目携带完整agent执行步骤，校验步骤长度合规
    - 结果：未通过
    - 失败原因：函数不识别db关键字参数，参数匹配报错
    - 后端修复：同上

### 模块缺陷总结
1. 缺陷等级：**P1阻塞**
2. 核心问题：`generate_questions` 函数入参破坏性变更，`/api/question/generate` 全部出题流程用例阻塞
3. 修复文件：`services/question_service.py`
4. 修复要求：恢复多位置参数、新增db兼容关键字参数

---
## 二、错题&错题本模块（mistake）
测试文件：`tests/test_mistake_*.py`
对应后端文件：`routers/mistake_router.py`、`services/mistake_service.py`、`services/mastery_service.py`
涉及接口：
- 错因确认：`POST /api/mistake/confirm`
- 错题详情：`GET /api/mistake/{id}`
- 错题重训练：`POST /api/mistake/retrain`
- 错题本列表：`GET /api/mistake/notebook`
- 相似错题检索：`GET /api/mistake/similar`

### 未通过用例明细
#### 1. 全链路错因闭环组
1. **test_confirm_cause_full_data_chain**
    - 场景：完整确认错因链路，校验错题入库、掌握度分值计算
    - 接口：`POST /api/mistake/confirm`
    - 结果：未通过
    - 失败原因：分值计算返回None，执行`None > 数字`对比触发TypeError
    - 后端修复：`mistake_service.py` 分值逻辑增加空兜底，无数据默认返回0，禁止返回None
2. **test_full_answer_mistake_auto_cycle**
    - 场景：答题-确认错因-自动生成错题完整闭环
    - 接口：`POST /api/mistake/confirm`
    - 结果：未通过
    - 失败原因：路由文件重命名，测试导入`routers.mistakes`不存在，AttributeError
    - 后端修复：方案二选一：① 修改测试导入为`routers.mistake_router`；② `routers/__init__.py` 增加别名`mistakes = mistake_router`
3. **test_mistake_switch_mastery_flow**
    - 场景：错题标记掌握/不熟，自动更新知识点状态、过滤错题本
    - 接口：`POST /api/mistake/confirm`
    - 结果：未通过
    - 失败原因：路由导入路径不存在报错
    - 后端修复：同上路由兼容方案
4. **test_retrain_stub_fixed_output**
    - 场景：错因推荐重训练，校验返回列表结构
    - 接口：`POST /api/mistake/retrain`
    - 结果：未通过
    - 失败原因：路由导入路径不存在报错
    - 后端修复：同上
5. **test_mistake_detail_404_when_not_exist**
    - 场景：查询不存在错题ID，校验标准404返回结构
    - 接口：`GET /api/mistake/{id}`
    - 结果：未通过
    - 失败原因：路由导入路径不存在报错
    - 后端修复：同上
6. **test_empty_notebook_no_mistake**
    - 场景：空白用户错题本，返回空数组无报错
    - 接口：`GET /api/mistake/notebook`
    - 结果：未通过
    - 失败原因：路由导入路径不存在报错
    - 后端修复：同上
7. **test_no_token_401_intercept**
    - 场景：无Token访问所有错题接口，校验全局鉴权拦截返回401
    - 接口：全部 `/api/mistake/*` 接口
    - 结果：未通过
    - 失败原因：错题接口未挂载全局鉴权，无Token仍返回200，存在越权安全漏洞
    - 后端修复：`mistake_router.py` 全部接口添加`Depends(get_current_user)`鉴权依赖

#### 2. 错题本筛选接口组
1. **test_manual_mark_unknown_in_notebook**
    - 场景：手动标记「不会」，校验不会题本正常查询
    - 接口：`GET /api/mistake/notebook`
    - 结果：未通过
    - 失败原因：未实现/未挂载`/api/mistake/notebook`接口，请求404，断言200失败
    - 后端修复：`mistake_router.py` 实现错题本列表接口并挂载，返回标准`{"ok":true,"data":{},"msg":""}`结构
2. **test_manual_mark_unfamiliar_in_notebook**
    - 场景：手动标记「不熟」，校验不熟题本筛选正常
    - 接口：`GET /api/mistake/notebook`
    - 结果：未通过
    - 失败原因：错题本接口404，返回JSON无顶层`data`字段，读取键触发KeyError
    - 后端修复：同上，补全并挂载错题本接口
3. **test_manual_mark_master_auto_remove**
    - 场景：标记为掌握，不会/不熟题本过滤该错题
    - 接口：`GET /api/mistake/notebook`
    - 结果：未通过
    - 失败原因：错题本接口404，无`data`字段KeyError
    - 后端修复：同上
4. **test_continuous_correct_auto_upgrade_status**
    - 场景：同一知识点连续答对3次，自动升级掌握状态、错题同步移出不熟/不会题本
    - 接口：`GET /api/mistake/notebook`、`POST /api/mastery/recalculate`
    - 结果：未通过
    - 失败原因：`recalculate_mastery`仅更新知识点表，未同步刷新Mistake表`mastery_status`，错题状态不更新，断言不匹配
    - 后端修复：`mastery_service.py` 重算逻辑增加同步更新Mistake表状态代码
5. **test_weak_point_only_kp_tag_not_in_notebook**
    - 场景：知识点判定为薄弱点，仅全局标签，未手动标记错题不进入错题本
    - 接口：`GET /api/mistake/notebook`
    - 结果：未通过
    - 失败原因：错题本接口404，无`data`字段KeyError
    - 后端修复：同上补全错题本接口

#### 3. 相似错题向量检索接口组
1. **test_similar_chroma_enabled_normal_query**
    - 场景：Chroma向量库正常，检索相似错题列表
    - 接口：`GET /api/mistake/similar`
    - 结果：未通过
    - 失败原因：未实现/未挂载`GET /api/mistake/similar`接口，请求404
    - 后端修复：`mistake_router.py` 实现相似错题接口，`main.py`完成路由挂载，返回标准200结构
2. **test_similar_chroma_disabled_fallback_same_kp**
    - 场景：Chroma离线，降级查询同知识点错题兜底
    - 接口：`GET /api/mistake/similar`
    - 结果：未通过
    - 失败原因：`/api/mistake/similar`接口不存在，404状态码断言失败
    - 后端修复：同上补全相似错题接口
3. **test_similar_no_matching_data_return_empty**
    - 场景：无匹配相似错题，返回空items数组
    - 接口：`GET /api/mistake/similar`
    - 结果：未通过
    - 失败原因：`/api/mistake/similar`接口不存在，404状态码断言失败
    - 后端修复：同上补全相似错题接口

### 错题本模块缺陷总结
1. P1阻塞：
    - 路由文件重命名，测试导入路径失效；
    - 缺失`/api/mistake/notebook`、`/api/mistake/similar`核心接口，错题本、相似错题全功能不可用；
    - 全部`/api/mistake/*`接口无全局Token鉴权，存在越权访问安全漏洞；
    - 掌握度重算逻辑不同步更新错题状态，错题本筛选逻辑失效。
2. P2业务缺陷：`POST /api/mistake/confirm`错因分值计算无空兜底，返回None导致对比报错。
3. 修复文件：`routers/mistake_router.py`、`services/mistake_service.py`、`services/mastery_service.py`、`routers/__init__.py`

---
## 三、知识点掌握度模块（mastery）
测试文件：`tests/test_mastery.py`
对应后端文件：`services/mastery_service.py`、`routers/mastery_router.py`
核心接口：
- 掌握度重算：`POST /api/mastery/recalculate`
- 批量同步知识点掌握状态：`POST /api/mastery/sync_all`
- 掌握度全流程查询：`GET /api/mastery/{kp_id}`

### 未通过用例明细
1. **test_mastery_status_priority_rule**
    - 场景：连续答错3题应判定为「薄弱点」，校验状态优先级规则
    - 接口：`POST /api/mastery/recalculate`
    - 结果：未通过
    - 失败原因：状态判定逻辑失效，满足薄弱点条件仍返回「未学」，断言不匹配
    - 后端修复：`mastery_service.py` 修复五档状态优先级，薄弱点优先级最高，连续答错3次强制输出「薄弱点」
2. **test_weak_score_calculate_weight**
    - 场景：校验错题加权计分公式，固定数据预期分值3
    - 接口：`POST /api/mastery/recalculate`
    - 结果：未通过
    - 失败原因：计分权重修改，计算结果与验收文档标准不符
    - 后端修复：还原标准加权计分公式，对齐需求分值规则
3. **test_sync_all_knowledge_mastery**
    - 场景：批量同步全知识点掌握状态，校验薄弱点标记生效
    - 接口：`POST /api/mastery/sync_all`
    - 结果：未通过
    - 失败原因：批量同步复用的判定逻辑失效，状态输出「未学」
    - 后端修复：同上统一修复状态优先级判定
4. **test_mastery_api_full_cycle**
    - 场景：掌握度接口完整闭环，校验无Token访问拦截401
    - 接口：全部 `/api/mastery/*` 接口
    - 结果：未通过
    - 失败原因：路由文件重命名为`mastery_router.py`，测试导入`routers.mastery`不存在；同时接口未挂载全局鉴权，无Token返回200
    - 后端修复：① 路由兼容别名；② `mastery_router.py` 全部接口添加鉴权依赖`Depends(get_current_user)`

### 掌握度模块缺陷总结
1. P1阻塞：
    - `POST /api/mastery/recalculate` 掌握状态优先级逻辑错误，连续答错无法识别薄弱点；加权计分规则与需求不符；
    - 全部`/api/mastery/*`接口无全局鉴权，存在越权漏洞；路由导入路径失效。
2. 修复文件：`services/mastery_service.py`、`routers/mastery_router.py`、`routers/__init__.py`

---
## 四、通用底层环境缺陷（SQLAlchemy模型加载冲突）
### 报错信息
`sqlalchemy.exc.InvalidRequestError: Table 'user' is already defined for this MetaData instance`
1. 成因：pytest多次重复加载`backend.models`，重复注册User数据表，同一MetaData实例表冲突
2. 根治修复方案：所有数据库模型类增加配置 `__table_args__ = {"extend_existing": True}`，允许重复加载复用表定义
3. 附加小问题：测试导入存在笔误`backend_models`，正确写法`backend.models`

---
## 全量阻塞缺陷总览
1. 出题模块：`POST /api/question/generate` 函数入参破坏性变更，全部出题用例阻塞（P1）
2. 错题模块：缺失`/api/mistake/notebook`、`/api/mistake/similar`核心接口；路由导入失效；全部错题接口无全局鉴权；掌握度重算不同步更新错题（P1）
3. 掌握度模块：`/api/mastery/*` 状态判定、计分规则逻辑错误；接口无鉴权、路由导入失效（P1）
4. 底层模型：SQLAlchemy重复加载表冲突，所有测试收集阶段报错阻塞执行（P1）


# Day4 接口自动化测试验收清单
## 一、视频资源模块
测试文件：`tests/test_video_api.py`，`ests/test_video_crawler.py`
对应后端接口文件：`routers/video_router.py`
验收场景：3 条视频 -> 按 quality_score 返回 top 3
         1 条视频 -> 返回已有，crawl_triggered=true
         0 条视频 -> fallback_used=true，不 500
         爬虫失败 -> 返回已有种子或空状态
测试结果：所有用例已全部通过
---
## 二、论坛模块
测试文件：`tests/test_forum_search.py`，`tests/test_forum_like.py`，`tests/test_forum_leaderboard.py`，`tests/test_forum_ai_personalized.py`
对应后端接口文件：`routers/forum_router.py`
验收场景：帖子发布、分页查询、评论点赞、用户帖子、分类筛选、异常兜底、无token鉴权拦截
测试结果：所有用例已全部通过

---
## 三、报告模块
测试文件：`tests/test_report_trends.py`，`tests/test_report_plan.py`，`tests/test_report_export.py`
对应后端接口文件：`routers/report_router.py`
验收场景规范：overview -> stats/subject_trends/profile/memory_weights/next_plan
             plan -> 每项有 subject/knowledge_point/count/difficulty
             export -> PDF 或 print fallback

### 未通过测试用例明细（仅2条导出相关用例失败，其余全部通过）
1. **test_report_export_pdf**
    - 测试场景：触发PDF导出，校验正常返回二进制文件、响应头`application/pdf`
    - 测试结果：未通过
    - 失败根因：后端缺失`/api/reports/export`导出接口，无PDF生成、文件返回逻辑
    - 后端修改方案：新增`GET /api/reports/export`导出路由，实现PDF二进制生成返回逻辑

2. **test_report_export_print_fallback**
    - 测试场景：print打印兜底导出，校验返回结构化文本print_content
    - 测试结果：未通过
    - 失败根因：后端缺失`/api/reports/export`导出接口，无print兜底分支逻辑
    - 后端修改方案：新增`GET /api/reports/export`导出路由，增加`export_type=print`文本兜底分支，返回print_content字段

### 报告模块总结缺陷
   缺陷等级：**P1阻塞**
#### 完全未实现、代码中不存在的接口
   **导出接口 `/api/reports/export`**（需求标注需支持PDF导出、print打印兜底），无任何路由、导出逻辑、文件生成代码；

---
## 四、OCR模块
测试文件：`tests/test_ocr_api.py`
对应后端接口文件：`routers/ocr_router.py`
验收场景：图片上传存储路径、OCR服务不可用降级兜底、分析写入mistake/user_memory、重复题目不无限新增错题
测试结果：所有用例已全部通过


# Day 5  验收清单
## 一、今日主流程
测试账号：demo@turing408.ai / 123456
测试范围：严格版 P5 后端接口契约回归，覆盖答题、错因推荐、首页、论坛AI、OCR、报告、视频等主流程模块
执行命令：`python -m pytest tests -q`
执行环境：Python 3.12.6，pytest 9.1.1
测试结论：测试可完整收集并运行，后端仍存在 7 个接口契约问题

## 二、自动化测试结果
全量测试（总计86条用例）：通过79条，失败7条

| 类型 | 数量 | 说明 |
| ---- | ---- | ---- |
| 通过 | 79 | 健康检查、认证、知识图谱、推荐、RAG、会话、出题、错题、掌握度、论坛基础、报告趋势/计划等主流程可用 |
| 失败 | 7 | 均为后端返回字段或异常兜底契约不一致 |
| 警告 | 410 | 主要为 `datetime.utcnow()` DeprecationWarning 和 SQLAlchemy 默认值警告，不阻塞业务验收 |

## 三、失败项明细

| 编号 | 测试用例 | 失败现象 | 复现方式 | 归属文件 | 是否阻塞明天 | 建议处理 |
| ---- | -------- | -------- | -------- | -------- | ------------ | -------- |
| 1 | `tests/test_answer_graph.py::test_answer_check_contract` | `/api/answers/check` 返回体缺少 `recommended_causes`，实际返回 `suggested_error_types` | 登录后生成题目并提交答案，校验答题结果字段 | `routers/answer_router.py`、答题服务层 | 是 | 在答题结果 `data` 中补齐 `recommended_causes` 字段，或将 `suggested_error_types` 映射为兼容字段 |
| 2 | `tests/test_cause_recommendation.py::test_answer_wrong_returns_recommended_causes` | 错题答案对象内缺少 `recommended_causes`，错因推荐契约不满足 | 调用答题辅助流程生成错误答案记录 | `routers/answer_router.py`、错因推荐服务 | 是 | 错题答题失败时统一返回 `recommended_causes`，保证前端错因推荐组件可直接读取 |
| 3 | `tests/test_forum_ai_personalizad.py::test_forum_ai_answer_personalized_section` | 论坛AI回答接口未返回 `personalized_section` | mock `ai_answer_for_post` 后调用 `/api/forum/posts/{post_id}/ai-answer` | `routers/forum_router.py` | 否 | 保留并透传 AI 服务返回的 `personalized_section` 字段 |
| 4 | `tests/test_home_personalization.py::test_home_overview_contract` | 首页接口返回 `memories`，但契约要求 `memory` | 登录后调用 `/api/home/overview` | `routers/home_router.py` | 是 | 首页返回体增加 `memory` 兼容字段，必要时保留 `memories` 作为旧字段 |
| 5 | `tests/test_ocr.py::test_ocr_upload_returns_path` | OCR上传返回 `stored_path`，但契约要求 `file_path` 或 `path` | 上传测试图片到 `/api/ocr/upload` | `routers/ocr_router.py` | 否 | 返回体补齐 `file_path` 或 `path`，值可复用 `stored_path` |
| 6 | `tests/test_report_export.py::test_report_history_print_fallback_contract` | `/api/reports/history` 返回500，异常为 `the JSON object must be str, bytes or bytearray, not list` | 登录后直接 GET `/api/reports/history` | `routers/report_router.py` | 是 | 修复历史报告 JSON 解析逻辑，兼容字段已是 list 的情况，避免重复 `json.loads` |
| 7 | `tests/test_video_api.py::test_video_click_contract` | `/api/videos/click` 传入不存在视频ID时返回500 | POST `/api/videos/click`，body 为 `{\"video_id\": 999999}` | `routers/video_router.py` | 否 | 视频点击接口对不存在ID做404/空状态兜底，禁止抛500 |

## 四、降级测试
1. **视频点击降级**：不存在的视频ID未做兜底，接口直接500，需降级为404或标准错误结构
2. **报告历史降级**：历史报告中列表字段重复 JSON 解析导致500，需兼容 list/dict/string 三种存储形态
3. **OCR上传降级**：上传主流程成功，但字段别名不兼容，前端读取 `file_path/path` 时会失败
4. **字段兼容降级**：首页 `memory/memories`、答题 `recommended_causes/suggested_error_types` 均属于新旧字段名不一致问题，建议新增兼容字段而不是删除旧字段

## 五、结论
1. 可以完整收集并运行，当前基线为 **86条用例，79通过，7失败**。
2. 7个失败项主要集中在后端接口契约：字段缺失、字段命名不一致、异常兜底返回500。
3. 明日优先修复 4 个阻塞项：答题 `recommended_causes`、错因推荐 `recommended_causes`、首页 `memory`、报告历史500。
4. 论坛AI个性化字段、OCR路径字段、视频点击不存在ID兜底为非核心阻塞，但会影响前端细节体验，建议一并修复。

## 六、未解决风险
| 风险项 | 等级 | 说明 |
| ------ | ---- | ---- |
| 答题接口缺少 `recommended_causes` | P1 | 答题后错因推荐组件无法读取标准字段 |
| 错因推荐链路缺少 `recommended_causes` | P1 | 错题归因闭环字段不完整，影响错题本后续分析 |
| 首页缺少 `memory` 字段 | P1 | 首页个性化记忆卡片按契约读取会失败 |
| 报告历史接口500 | P1 | 报告历史页面无法稳定打开 |
| 论坛AI缺少 `personalized_section` | P2 | 个性化追问/薄弱点推荐无法展示 |
| OCR上传路径字段不兼容 | P2 | 上传成功但前端无法按标准字段取文件路径 |
| 视频点击不存在ID返回500 | P2 | 用户点击失效/下架视频时后端异常，不满足接口健壮性要求 |
