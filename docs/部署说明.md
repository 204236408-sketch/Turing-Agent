# 部署说明

## 本地演示

```powershell
python -m pip install -r backend\requirements.txt
cd backend
python -m uvicorn main:app --reload --host 127.0.0.1 --port 8000
```

## MySQL 切换

1. 创建数据库 `yantu408`。
2. 复制 `backend/.env.example` 为 `backend/.env`。
3. 配置：

```text
MYSQL_URL=mysql+pymysql://root:password@127.0.0.1:3306/yantu408?charset=utf8mb4
```

<<<<<<< HEAD
=======
## 数据库迁移

启动后端前先显式执行迁移，避免应用启动时自动改表：

```bash
cd backend
python scripts/migrate_db.py
python scripts/init_db.py
```

生产环境不要开启 `AUTO_MIGRATE_ON_STARTUP`。

>>>>>>> 2dbf2d9 (郭晶-6.26上午-修改版)
4. 安装 PyMySQL：

```powershell
python -m pip install pymysql
```

5. 重启后端。

## Nginx

参考 `deploy/nginx.conf`，把 `/api` 代理到后端，静态页面指向项目 `frontend` 目录。
