# 部署说明

1. 准备 MySQL 8 和 Python 3.11+。
2. 将项目部署到 `/opt/turing408`。
3. 配置 `backend/.env`。
4. 安装 `backend/requirements.txt`。
5. 执行数据库迁移和初始化脚本。
6. 使用 Uvicorn/Gunicorn 运行 FastAPI。
7. 使用 `deploy/nginx.conf` 托管前端并转发 `/api/`。

也可使用 `deploy/docker-compose.yml` 启动开发环境。

