# P1 后端总装与接口工作手册（综合新版）

> 角色定位：后端总装、API 契约、路由编排、MySQL 切换、云部署。  
> 上游：P2 数据库与数据资源、P3 Agent/RAG/爬虫。  
> 下游：P4 前端交互、P5 测试验收。
>
> 注意：本版基本保留原 P1 Day 1 工作，只在后续天数补齐 PDF 差距审计中要求的 MySQL、Chroma/RAG、LangGraph 接口适配、视频爬虫、论坛、报告导出和云部署任务。

---

## 0. P1 无痛执行区

这一节是 P1 每天开始前先看的执行卡。P1 的任务不是“把后端都写完”，而是把 P2 的数据和 P3 的智能函数变成 P4 能调用、P5 能测试的稳定 API。

### 0.1 今天开始前先确认

| 要确认的事 | 找谁拿 | 没拿到怎么办 |
|---|---|---|
| 今天用到的表和字段 | P2 | 先用现有模型写接口草稿，但不能承诺字段稳定 |
| 今天用到的 Agent/RAG/爬虫函数 | P3 | 先定义接口契约和空 fallback，等 P3 返回样例再接入 |
| P4 今天要接的页面 | P4 | 优先给页面最小可用字段，不做多余接口 |
| P5 今天要测的流程 | P5 | 把该流程接口优先补齐，其他接口可延期 |

### 0.2 P1 每天固定执行顺序

1. 上午 9:30 前，列出今天要开放的接口。
2. 上午先写或更新 `docs/接口文档.md` 的接口契约。
3. 中午前和 P2/P3 对齐字段，不确定的字段标注“待确认”。
4. 下午把 P3 函数接进 router，保证成功和失败都返回统一格式。
5. 16:00 联调时给 P4 一个可调用接口，哪怕只有最小字段。
6. 晚上 21:00 前给 P5 接口路径、请求体、返回体、错误码和测试账号。
7. 如果接口没完成，必须写明是缺数据、缺函数、缺字段，不能只写“未完成”。

### 0.3 P1 交付模板

每天收工时，在当天任务下补下面这段。

```md
### P1 今日交付

接口文档位置：docs/接口文档.md

已完成接口：
- `方法 路径`：用途；是否需要 token；P4 用在哪个页面；P5 用哪个测试验证

接口返回关键字段：
- 字段名：含义；来源于 P2/P3 哪个产物；为空时返回什么

失败和降级：
- 错误码：
- fallback 行为：
- 是否会返回 500：

交付给：
- P4：页面接入方式
- P5：测试入口和样例

未完成/阻塞：
- 阻塞原因：
- 责任人：
- 明天处理方式：
```

### 0.4 P1 完成标准

P1 的“完成”必须同时满足：

| 层级 | 完成标准 |
|---|---|
| 接口 | 路由已注册，能用 `/docs` 或测试客户端调用 |
| 契约 | `docs/接口文档.md` 写清请求体、返回体、错误码、是否登录 |
| 数据 | 字段和 P2 表结构一致，不凭空造字段 |
| Agent | 接入 P3 函数，函数失败时有 fallback，不直接 500 |
| 前端 | P4 知道页面应该调哪个接口、拿哪个字段 |
| 测试 | P5 至少能跑一个正常场景和一个失败/空状态场景 |

不算完成：

- 只有函数，没有路由。
- 只有路由，没有接口文档。
- 成功时能返回，失败时直接抛异常。
- 返回 JSON 字段临时命名，P4/P5 不知道含义。

### 0.5 新 P1 接手时怎么做

1. 先打开 `docs/验收清单.md`，看昨天失败项里属于 P1 的部分。
2. 打开 `docs/接口文档.md`，确认接口契约是否和代码一致。
3. 跑 `python -m pytest tests/test_smoke.py`，确认后端可启动。
4. 找 P2 要字段变更，找 P3 要函数返回样例。
5. 先修阻塞 P4/P5 的接口，再做新增接口。

### 0.6 P1 遇到阻塞怎么处理

P1 最常见阻塞是缺字段、缺 Agent 返回、接口契约变更。不要等到晚上才说。

```text
阻塞点：
影响哪个接口：
影响哪个页面/测试：
需要 P2/P3/P4/P5 提供什么：
临时 fallback：
最晚需要时间：
```

超过 30 分钟还不能解决时，先给 P4/P5 一个明确 fallback，例如返回空列表、降级标识或固定错误码，但必须写进接口文档。

### 0.7 P1 完成后上传到 Gitee

按 `docs/Gitee提交流程.md` 上传。P1 通常只提交：

```text
backend/routers/
backend/schemas.py
backend/config.py
backend/database.py
backend/main.py
deploy/
docs/接口文档.md
docs/云部署说明.md
```

上传前必须确认：

```powershell
git status --short
```

只添加自己负责的文件，例如：

```powershell
git add backend/routers/question_router.py docs/接口文档.md
git commit -m "P1 Day3: 完善出题接口契约"
git push -u origin p1-day3-question-api
```

不要提交 P2 数据文件、P3 Agent 逻辑、P4 页面文件，除非已经和对应负责人确认。

---

## 1. P1 整体负责范围

P1 不负责写复杂算法，也不负责准备题库数据。P1 的核心价值是把 P2 和 P3 的产物包装成稳定接口，让 P4 能接、P5 能测、云端能跑。

| 负责部分 | 具体职责 | 主要文件 |
|---|---|---|
| 路由总装 | 所有 router 注册、接口参数校验、返回格式稳定 | `backend/main.py`、`backend/routers/*.py` |
| API 契约 | 请求体、返回 JSON、错误码、是否需要登录 token | `backend/schemas.py`、`backend/utils/response.py`、`docs/接口文档.md` |
| MySQL 切换 | 支持 `MYSQL_URL`、MySQL Docker、云端连接 | `backend/config.py`、`backend/database.py`、`deploy/docker-compose.yml` |
| 视频接口 | 推荐、列表、爬虫触发、点击统计 | `backend/routers/video_router.py`、`backend/routers/question_router.py` |
| 出题接口 | 出题、智能推荐、提示、视频、收藏、交互记录 | `backend/routers/question_router.py` |
| 答题/错题接口 | 批改返回、错题详情、相似错题、题本筛选 | `backend/routers/answer_router.py`、`backend/routers/mistake_router.py` |
| 问答接口 | 结构化 QA、会话上下文、摘要、追问建议 | `backend/routers/qa_router.py`、`backend/routers/conversation_router.py` |
| 论坛接口 | 搜索分页、点赞收藏幂等、排行榜、打卡 | `backend/routers/forum_router.py` |
| 报告接口 | 概览、历史、结构化计划、导出 | `backend/routers/report_router.py` |
| 部署 | Nginx、systemd、Docker Compose、日志、环境变量 | `deploy/*`、`docs/云部署说明.md` |

---

## 2. 上下游先后顺序

```text
P2 先交：表结构、字段、种子数据、导入方式
P3 再交：Agent 函数、RAG/Chroma 查询、爬虫函数、fallback 行为
P1 包装：API 路由、统一返回、异常处理、接口文档
P4 接收：接口地址、请求体、返回 JSON，做页面
P5 验收：接口测试、全链路测试、云端测试
```

P1 每天必须向下游交付：

1. 接口路径。
2. 请求方式。
3. 请求体示例。
4. 成功返回示例。
5. 失败返回示例。
6. 是否需要登录。
7. 可否在 `/docs` 里测试。

---

## 3. 统一接口规则

所有接口必须统一返回。

成功：

```json
{
  "ok": true,
  "message": "success",
  "data": {}
}
```

失败：

```json
{
  "ok": false,
  "error": {
    "code": "QUESTION_NOT_FOUND",
    "message": "题目不存在",
    "detail": {},
    "request_id": "..."
  }
}
```

错误码规则：

| 状态码 | 用途 |
|---|---|
| `200` | 成功 |
| `400` | 业务参数错误 |
| `401` | 未登录、token 过期 |
| `403` | 无权限 |
| `404` | 资源不存在 |
| `422` | Pydantic 字段校验失败 |
| `500` | 未预期异常，不能用来表达普通业务失败 |

---

## 4. 每天工作安排

## Day 1：接口盘点和 MySQL 生产配置准备

> 这一天尽量沿用原手册，不大改。目标是把接口和部署地基摸清楚。

### 上午要做

1. 打开 `backend/main.py`，记录当前注册了哪些 router。
2. 打开 `backend/routers/*.py`，把接口分成三类：
   - 已完成可复用。
   - 半成品需要深化。
   - Mock/占位接口。
3. 检查 `backend/utils/response.py` 是否已有统一成功/失败返回。
4. 更新 `docs/接口文档.md`，先列出核心接口清单。

### 下午要做

1. 检查 `backend/requirements.txt` 是否包含：

```text
pymysql
cryptography
chromadb
paddleocr
paddlepaddle
apscheduler
langchain
langchain-core
langchain-community
langchain-openai
langgraph
beautifulsoup4
requests
numpy
```

2. 修改 `backend/.env.example`，补齐：

```text
MYSQL_URL=mysql+pymysql://root:password@127.0.0.1:3306/yantu408?charset=utf8mb4
CHROMA_PATH=./vector_store/chroma
SILICONFLOW_API_KEY=
SILICONFLOW_BASE_URL=https://api.siliconflow.cn/v1
SILICONFLOW_MODEL=deepseek-ai/DeepSeek-V3
CORS_ORIGINS=*
UPLOAD_DIR=./uploads
REPORT_DIR=./outputs/reports
```

3. 修改 `deploy/docker-compose.yml`，在原 backend 服务基础上增加 MySQL 服务。

参考格式：

```yaml
services:
  mysql:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root_password
      MYSQL_DATABASE: yantu408
      MYSQL_USER: yantu_user
      MYSQL_PASSWORD: yantu_password
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  backend:
    image: python:3.11-slim
    working_dir: /app/backend
    volumes:
      - ..:/app
    command: sh -c "pip install -r requirements.txt && python -m uvicorn main:app --host 0.0.0.0 --port 8000"
    ports:
      - "8000:8000"
    environment:
      - MYSQL_URL=mysql+pymysql://yantu_user:yantu_password@mysql:3306/yantu408?charset=utf8mb4
      - CHROMA_PATH=/app/backend/vector_store/chroma
    depends_on:
      - mysql

volumes:
  mysql_data:
```

### 晚上验收

```powershell
python -m pip install -r backend\requirements.txt
python -m pytest tests/test_smoke.py
```

### 交付给谁

- 给 P2：后端需要补齐的表清单。
- 给 P3：Agent 返回值应该满足的接口字段。
- 给 P4：接口文档初版。
- 给 P5：当天可测接口清单。

### 完成标准

- `docs/接口文档.md` 有接口清单。
- `deploy/docker-compose.yml` 有 MySQL。
- 本地后端仍能启动。

---

## Day 2：首页、知识图谱、问答接口真实化

### 上午要做

修改：

```text
backend/routers/home_router.py
backend/routers/knowledge_router.py
```

1. `GET /api/home/overview` 返回：

```json
{
  "today_plan": {},
  "countdown": {},
  "recommendations": [],
  "stats": {"cards": []},
  "knowledge_graph": {},
  "memories": [],
  "initial_state": {}
}
```

2. `today_plan` 必须包含可直接出题字段：

```json
{
  "subject": "操作系统",
  "knowledge_point": "页面置换算法",
  "mode": "薄弱点强化",
  "difficulty": "中等",
  "question_type": "选择题",
  "count": 3,
  "title": "今天优先攻克\n页面置换算法",
  "reason": "..."
}
```

3. `stats.cards` 增加趋势字段：

```json
{
  "label": "本周答题",
  "value": 36,
  "delta": "27 道正确",
  "trend": "+12%"
}
```

4. `GET /api/knowledge/graph?scope=all` 返回树状结构：

```json
{
  "subjects": [
    {
      "name": "操作系统",
      "children": [
        {
          "name": "内存管理",
          "children": [
            {
              "name": "页面置换算法",
              "status": "不熟",
              "style": {"color": "#d9a441"}
            }
          ]
        }
      ]
    }
  ]
}
```

### 下午要做

修改：

```text
backend/routers/qa_router.py
backend/routers/conversation_router.py
```

1. `POST /api/qa/chat` 返回结构化数据：

```json
{
  "conversation_id": 1,
  "subject": "操作系统",
  "knowledge_point": "页面置换算法",
  "answer": {},
  "agent_steps": [],
  "retrieved_knowledge": [],
  "memories_used": [],
  "suggested_followups": [],
  "llm_used": true
}
```

2. 新增或完善：

```text
GET /api/conversation/{conversation_id}/context
POST /api/conversation/{conversation_id}/summary
```

### 交付给谁

- 给 P4：首页和 QA 页面字段。
- 给 P5：首页、知识图谱、QA 测试入口。

### 完成标准

- 首页接口不依赖前端固定 Mock。
- QA 能返回 `agent_steps`。
- 无 Chroma/无 LLM 时不 500。

---

## Day 3：出题、批改、错题本接口

### 上午要做

修改：

```text
backend/routers/question_router.py
```

需要补齐接口：

```text
POST /api/questions/generate
POST /api/questions/generate-smart
GET  /api/questions/recommendations
GET  /api/questions/{question_id}/adaptive-hints
GET  /api/questions/{question_id}/videos
POST /api/questions/{question_id}/collect
POST /api/questions/{question_id}/interaction
```

`/{question_id}/videos` 必须按：

```text
subject + knowledge_point 精确查
→ 同 subject fallback
→ 空状态
```

收藏接口必须幂等：

```text
未收藏 -> 收藏
已收藏 -> 取消或标记 inactive
同一 user_id + question_id 不允许多条 active 记录
```

### 下午要做

修改：

```text
backend/routers/answer_router.py
backend/routers/mistake_router.py
```

1. `POST /api/answers/check` 返回：

```json
{
  "answer_record_id": 20,
  "is_correct": false,
  "score": 0,
  "feedback": {},
  "recommended_causes": ["审题错误", "规则混淆", "步骤遗漏"],
  "mastery": {}
}
```

2. 错题本接口：

```text
GET /api/mistakes/notebook?status=不熟
GET /api/mistakes/notebook?status=不会
GET /api/mistakes/{mistake_id}
POST /api/mistakes/similar
```

3. 错题详情必须返回：

```json
{
  "id": 1,
  "question_id": 10,
  "subject": "操作系统",
  "knowledge_point": "页面置换算法",
  "question_text": "...",
  "options": [],
  "standard_answer": "C",
  "user_answer": "B",
  "error_type": "规则混淆",
  "error_reason": "...",
  "suggestion": "...",
  "similar_mistakes": []
}
```

### 交付给谁

- 给 P4：出题、批改、错题本接口。
- 给 P5：答题闭环测试入口。

### 完成标准

- 手工能跑通：生成题目 -> 提交错题 -> 确认错因 -> 错题本可见。
- 收藏接口不会重复插入。

---

## Day 4：视频、论坛、报告接口

### 上午要做：视频接口

修改：

```text
backend/routers/video_router.py
backend/scheduler.py
backend/main.py
```

接口：

```text
GET  /api/videos/recommend?subject=&knowledge_point=&limit=3
GET  /api/videos/list?subject=&knowledge_point=
POST /api/videos/crawl
POST /api/videos/view/{video_id}
```

返回：

```json
{
  "items": [],
  "crawl_triggered": false,
  "fallback_used": false,
  "source": "database"
}
```

`POST /api/videos/crawl` 不能再返回“开发版不爬取”，必须调用 P3 的爬虫服务；爬虫失败时返回错误信息但不影响已有数据。

### 下午要做：论坛和报告

修改：

```text
backend/routers/forum_router.py
backend/routers/report_router.py
```

论坛接口：

```text
GET  /api/forum/posts?q=&category=&page=1&size=20
POST /api/forum/posts/{post_id}/like
POST /api/forum/posts/{post_id}/unlike
POST /api/forum/posts/{post_id}/collect
GET  /api/forum/leaderboard?scope=daily&limit=10
GET  /api/forum/hot
GET  /api/forum/checkin/status
GET  /api/forum/checkin/stats
```

点赞规则：

```text
同一 user_id + post_id 只能 active 点赞一次
重复点赞不增加 like_count
取消点赞后 like_count -1，但不能低于 0
```

报告接口：

```text
GET  /api/reports/overview
POST /api/reports/generate
GET  /api/reports/history
GET  /api/reports/{report_id}/export?format=pdf
```

报告导出 fallback：

```json
{
  "downloadable": false,
  "fallback": "print",
  "html": "<html>...</html>"
}
```

### 完成标准

- 视频、论坛、报告接口可在 `/docs` 调用。
- 点赞幂等。
- 报告计划是结构化数组。

---

## Day 5：云部署与最终联调

### 上午要做

修改：

```text
deploy/docker-compose.yml
deploy/nginx.conf
deploy/yantu408.service
deploy/start.sh
deploy/restart.sh
docs/云部署说明.md
```

云端环境变量：

```text
MYSQL_URL=
SECRET_KEY=
SILICONFLOW_API_KEY=
SILICONFLOW_BASE_URL=
SILICONFLOW_MODEL=
CHROMA_PATH=
CORS_ORIGINS=
UPLOAD_DIR=
REPORT_DIR=
```

### 下午要做

1. 配合 P2 导入 MySQL 数据。
2. 配合 P3 导入 ChromaDB。
3. 配合 P4 打开前端页面。
4. 配合 P5 跑云端主流程。

检查：

```text
http://服务器IP/version-b.html
http://服务器IP/api/health
```

`/api/health` 至少返回：

```json
{
  "ok": true,
  "data": {
    "database": "mysql",
    "llm_enabled": true,
    "chroma": {"enabled": true}
  }
}
```

### 完成标准

- 云端前端可访问。
- 云端后端可访问。
- MySQL 重启数据不丢。
- Nginx 正常代理 `/api`。

---

## 5. P1 每日自查清单

- 今天新增接口是否写入 `docs/接口文档.md`？
- 接口是否有成功和失败示例？
- 失败是否避免无意义 500？
- P4 是否已经拿到字段？
- P5 是否知道怎么测？
- 上游 P2/P3 是否有阻塞？

## 6. P1 最终交付物

- 完整后端 API。
- MySQL Docker/云端配置。
- Nginx/systemd 配置。
- 接口文档。
- 云部署说明。
- 可供 P4/P5 使用的稳定接口契约。
